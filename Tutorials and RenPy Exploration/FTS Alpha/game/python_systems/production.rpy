init offset = -1
init python:
    #initalize aspects of the game

    visual_novel = VisualNovelManager()
    forum_manager = ForumManager()
    amelie_profile = ForumProfile()

    print (visual_novel.name)
